# Phase 3 (backend agent): write ingestion tests here
# These tests should cover: PDF parsing, text chunking, ChromaDB storage

import pytest


@pytest.mark.asyncio
async def test_placeholder() -> None:
    """Placeholder — backend agent: replace with real ingestion tests."""
    assert True
